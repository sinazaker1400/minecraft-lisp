minecraft simulation, qwen ai helped

the commands for terminal:

to navigate to the file destination :
cd ~/quicklisp/local-projects/minecraft-lisp


to run the project:

sbcl --load ~/quicklisp/setup.lisp --eval '(require :asdf)' --eval '(asdf:load-system :minecraft)' --eval '(minecraft-3d:start-game)'

to commit:
git -c http.proxy= -c https.proxy= push -u origin main
